{% extends "base.twig" %}

{% block content %}

    {{template_uri}}
    <div class="grid">
        {% for post in posts %}
            {% include ['gallery-item.twig', 'tease.twig'] %}
        {% endfor %}
    </div>
{% endblock %}